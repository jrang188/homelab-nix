keys:
  - &test_vm age1ts9g23ws6rfc3ktfhgpeea94ktqszz78vy93r2w07l6geczxqa4qyug57p
  # - &k3s_agent_hml age1REPLACE_ME  # derived from the real host's SSH host
  #   key via `ssh-to-age` after nixos-anywhere installs it; fill in before
  #   creating hosts/k3s-agent-hml/secrets.yaml

creation_rules:
  - path_regex: tests/vm/fixtures/secrets\.yaml$
    key_groups:
      - age:
          - *test_vm

  # - path_regex: hosts/k3s-agent-hml/secrets\.yaml$
  #   key_groups:
  #     - age:
  #         - *k3s_agent_hml
